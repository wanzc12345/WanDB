#ifndef CRYPTOGRAPHY_H
#define CRYPTOGRAPHY_H
class Cryptography{
private:
    int seed;
public:
    Cryptography();
    ~Cryptography();
    bool encrypt(char* filename);
    bool encrypt(char* inText, char* outText);
    bool decrypt(char* filename);
    bool decrypt(char* inText, char* outText);
};
#endif // CRYPTOGRAPHY_H
